# Task list: Making the paper consistent with the AI framing

---

## Tasks

### 1. Add AI adoption parameter to the Model section (Sec 4)

**File:** `doc/tex/main.tex` (lines ~416--1037)

Add a new subsection (e.g., "AI as an Asymmetric Shock to the Model") at the end of Section 4, after the baseline model is fully specified. This subsection:

- Introduces $a \in [0,1]$ and its interpretation (AI adoption intensity)
- Defines the four functional forms:
  - $V(x, a) = V(x) \cdot (1 + \gamma a)$
  - $K_{rev}(a) = K_{rev}(0) \cdot (1 + \delta a)$, with $\delta < \gamma$
  - $c(a)$ decreasing in $a$
  - $\lambda(a)$ increasing in $a$
- Shows that $\Pi_{bad}(x, a) = r \cdot \bar{\rho}(x,a) \cdot \pi_0(x,a) - c(a)$ is increasing in $a$ (the core asymmetry result)
- Explains that the baseline model (Sec 5 analysis) corresponds to $a = 0$, and later sections examine what happens as $a$ rises

This keeps the existing model intact and adds AI as a parameterisation layer, consistent with the rewrite guide's Section 5.

**Effort:** Medium (1--2 pages of new LaTeX)

---

### 2. Update Dynamics section (Sec 5) prose to reference AI

**File:** `doc/tex/main.tex` (lines ~1040--1280)

The mathematical content stays the same -- the replicator dynamics and regime classification are valid for any fixed parameter set, including one generated by a specific value of $a$. Changes:

- Add a framing paragraph at the start of Sec 5 explaining that the dynamics below hold for any fixed $a$, and that the regime the system occupies depends on how $a$ shifts the parameters
- In the regime classification table (Table 3), add a sentence noting that increasing $a$ moves the system across regime boundaries
- Add forward references: "Section 6 examines how these regime boundaries shift as AI adoption $a$ increases"

**Effort:** Low (paragraph-level additions, no new math)

---

### 3. Add AI-parameterised phase diagram analysis (Sec 6)

**File:** `doc/tex/main.tex` (Parameter Sensitivity section, lines ~1280+)

Add a new subsection to Section 6 that shows how the phase diagram shifts with $a$. Specifically:

- Show the $(K_{rev}, K_{store})$ phase diagram at $a = 0$ (baseline) and $a = 0.5$ or $a = 1.0$ (high AI adoption)
- The key visual: the Green (honesty) region shrinks and the Red (corruption) region expands as $a$ rises, because higher $a$ simultaneously increases volume, lowers cost, and raises $\lambda$
- Add a figure showing $x^*(a)$: the stable equilibrium as a function of $a$, demonstrating the tipping point
- Discuss the comparative statics: which channel of AI matters most? (likely $\lambda$, consistent with the existing screening sensitivity result)

**Effort:** Medium-High (new analysis + 2 new figures)

---

### 4. Add hysteresis-widening-with-$a$ analysis (Sec 6)

**File:** `doc/tex/main.tex` (Hysteresis subsection)

The existing hysteresis analysis sweeps $N$. Add a parallel analysis sweeping $a$:

- Show the hysteresis loop in $(a, x_{st})$ space: as $a$ increases from 0, quality is maintained until a collapse threshold $a_{\uparrow}$; recovery requires reducing $a$ below a distinct threshold $a_{\downarrow} < a_{\uparrow}$
- This directly delivers the Introduction's promise that "gradual AI adoption can trigger a discrete tipping point" and "the value of $a$ at which quality collapses is strictly lower than the value at which quality recovers"
- Add one new figure showing this hysteresis loop in $a$

**Effort:** Medium (new figure + ~1 page of text)

---

### 5. Create Python script for AI-parameterised figures

**File:** New script `scr/AI_adoption_analysis.py`

Implement the AI extension computationally:

- Define $c(a)$, $\lambda(a)$, $K_{rev}(a)$, $V(x,a)$ with elasticity parameters $\gamma, \delta$
- Generate Figure: Phase diagram at $a=0$ vs $a=0.5$ vs $a=1.0$ (side-by-side or overlay)
- Generate Figure: $x^*(a)$ tipping curve -- stable equilibrium vs $a$
- Generate Figure: Hysteresis loop in $(a, x_{st})$ space
- Reuse the existing `soft_min` and regime-classification logic from `Boundaries_regimes.py` and `Hysteresis_Loop_and_Quality_Collapse.py`

**Effort:** Medium-High (new script, ~200--300 lines, building on existing code)

---

### 6. Update ABM section to include AI adoption sweep

**Files:** `doc/tex/main.tex` (Sec 7), possibly `scr/ABM_authors.py`

The ABM currently validates the static model at two fixed parameter sets. Extend it:

- Add a simulation where $a$ increases gradually over time (e.g., linearly from 0 to 1 over 5000 timesteps), and show that the ABM reproduces the tipping point predicted by the ODE analysis
- Alternatively, run the ABM at several fixed values of $a$ and show that the regime transitions match the analytical predictions
- Add one new figure showing the ABM time series with increasing $a$

**Effort:** Medium (modify existing ABM script + new figure + ~0.5 page of text)

---

### 7. Rewrite remaining prose sections for style consistency

**File:** `doc/tex/main.tex`

Sections 4--7 still use the old voice (impersonal, jargon-heavy, specification-style). Apply the same style-guide treatment that was done for Sections 1--3:

- "We" voice throughout
- Contrastive framing at transitions
- Accessible topic sentences
- Lower-case paragraph headings reading as topic sentences
- Direct for results ("We find"), hedged for interpretation ("This suggests")
- One sentence per line

**Sections in scope:** Model (Sec 4 prose, not equations), Dynamics (Sec 5 prose), remaining Parameter Sensitivity prose, ABM prose, Co-operation ABM, Journal as Evolutionary Agent.

**Effort:** Medium (similar scope to the rewrite already done for Sections 1--3)

---

### 8. Write the Conclusion (Sec 8)

**File:** `doc/tex/main.tex` (currently empty at line ~1632)

The Conclusion must:

- Restate the AI asymmetry framing: AI scales production faster than verification
- Summarize the three main results (scarcity stability, tipping points, hysteresis) now explicitly in terms of $a$
- Connect back to the four channels from the Introduction
- State the policy implication: verification-centric institutions (audits, submission quotas, decoupled certification), not more slots
- Mention limitations: single-journal model, AI as scalar, no endogenous reviewer incentives
- Suggest 2--3 extensions: multi-journal competition, endogenous AI adoption (payoff-driven), reviewer incentive design

Follow the style guide: "we" voice, contrastive framing, 2--3 paragraphs, no generic future-work laundry list.

**Effort:** Medium (2--3 paragraphs, ~1 page)

---

### 9. Add mechanism diagram (Figure 1)

**File:** `doc/tex/main.tex` (end of Introduction, line ~120)

The rewrite guide calls for a mechanism diagram showing:
AI adoption ($a$) → four arrows → ($V$ up, $c$ down, $\lambda$ up, $K_{rev}$ slow) → $\Pi_{bad}$ up → tipping

This can be done as a TikZ figure directly in LaTeX (the paper already uses TikZ). It visually anchors the entire argument and gives the reader a map before entering the formal model.

**Effort:** Low-Medium (TikZ diagram, ~30--50 lines)

---

### 10. Reconcile the Introduction's $\Pi_{bad}(x, a)$ with the Model's $\Pi_{bad}(x)$

**File:** `doc/tex/main.tex`

Right now, the Introduction (line ~87) writes $\Pi_{bad}(x, a) = r \cdot \bar{\rho}(x,a) \cdot \pi_0(x,a) - c(a)$, but the Model section (line ~1096) defines $\Pi_{bad}(x) = r \cdot \bar{\rho}(x) \cdot \pi_0(x) - c$. These must be reconciled:

- The Model section should define $\Pi_{bad}(x)$ as the baseline payoff at $a = 0$
- The AI extension subsection (Task 1) should then show how $\Pi_{bad}(x, a)$ generalises this
- Ensure notation is consistent throughout: either always write the $a$ dependence or explicitly state when $a$ is held fixed

**Effort:** Low (notation pass through the paper)

---

### 11. Review and update the Co-evolutionary ABM and Journal Agent sections

**File:** `doc/tex/main.tex` (lines ~1543--1628)

These sections (Co-operation ABM, Journal as Independent Evolutionary Agent) discuss batch-matching ABM and journal strategy evolution but make no reference to AI. Decide:

- Either integrate AI adoption into these extensions (e.g., journal agents adapt screening in response to AI-driven volume increases)
- Or frame them explicitly as "further extensions beyond the AI analysis" so the reader understands their relationship to the main thread

**Effort:** Low-Medium (framing + possibly light model tweaks)

---

## Suggested order of execution

| Priority | Task | Dependency |
|----------|------|------------|
| 1 | Task 1: Add $a$ to Model | None |
| 2 | Task 10: Reconcile notation | Task 1 |
| 3 | Task 2: Update Dynamics prose | Task 1 |
| 4 | Task 5: Python script for AI figures | Task 1 |
| 5 | Task 3: AI phase diagram analysis | Tasks 1, 5 |
| 6 | Task 4: Hysteresis widening with $a$ | Tasks 1, 5 |
| 7 | Task 9: Mechanism diagram | None (can be done anytime) |
| 8 | Task 6: ABM with AI sweep | Tasks 1, 5 |
| 9 | Task 7: Style-guide rewrite of Secs 4--7 | Tasks 1--6 |
| 10 | Task 8: Write Conclusion | Tasks 1--6 |
| 11 | Task 11: Co-evolutionary / Journal Agent sections | Tasks 1, 7 |

## Verification

After all tasks:

1. `cd doc/tex && pdflatex main.tex && bibtex main && pdflatex main.tex && pdflatex main.tex` — no errors
2. Search for orphaned references to the old title or framing
3. Grep for $\Pi_{bad}(x)$ without $a$ — should only appear in baseline-specific context
4. Run all Python scripts and confirm figures regenerate
5. Read the paper linearly: does the AI thread run from Abstract through Conclusion without gaps?
6. Check against `AI_paper_rewrite_guide.md` Section 6 checklist
